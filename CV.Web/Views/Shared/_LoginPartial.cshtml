@if (Request.IsAuthenticated)
{
    <text>Welcome <strong>@User.Identity.Name</strong> [ @Html.ActionLink("Log Off", "LogOff", "User") ]</text>
}
else
{
    <text> [ @Html.ActionLink("Login", "LogOn", "User") ] , [ @Html.ActionLink("Register", "Register", "User") ]</text>
}
